const LIMITS_LISTS = [
  { label: "8", value: 8 },
  { label: "12", value: 12 },
  { label: "16", value: 16 },
];

const LIMIT_DEFAULT = LIMITS_LISTS[0].value;

const PAGE_DEFAULT = 1;

const DELAY = 1000;

export { LIMITS_LISTS, DELAY, PAGE_DEFAULT, LIMIT_DEFAULT };
